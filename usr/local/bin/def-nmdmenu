#!/bin/env bash

source /usr/local/bin/colors

FONT="Iosevka Term:size=12"

# configuration of networkmanager-dmenu
networkmanager_dmenu -c -i -l 10 -nb "$bg" -nf "$fg" -sb "$altbg" -sf "$fg" -fn "$FONT"
